(function (global) {
	'use strict';

	global.Timer = function (options) {

		let noop = function () {},
			minutes = options.minutes || 5,
			seconds = options.seconds || 0,
			onEverySecond = function () {}

	};

})(window);
